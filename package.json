{
  "name": "image-node-events-buffer-threads",
  "version": "1.0.0",
  "description": "**Here's a challenge that incorporates these concepts without involving audio:**",
  "main": "index.js",
  "scripts": {
    "dev:ts": "ts-node ./src/index.ts",
    "dev": "npm run build && npm run start",
    "build": "npx rimraf ./build && tsc --skipLibCheck",
    "start": "node ./build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^20.10.4"
    
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "node-gyp": "^10.0.1",
    "sharp": "^0.33.1"
  }
}
